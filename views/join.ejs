<%- contentFor('page') %>

<h1 style="text-align: center"><%= title %></h1>
<% if (typeof user==='undefined') { %>
<p>You're not even logged in and you wanna join the club? I don't think so, go <a href="/">back</a> now.</p>
<% } else { %>
<% if (user.status==='Anonymous') { %>
<% if (typeof errors==='undefined') { %>
<form method="post">
  <div class="form-group">
    <label for="secretPassword">What is the name of the first Lord of Cinder?</label>
    <input type="password" class="form-control" id="secretPassword" name="secretPassword" required>
  </div>
  <button type="submit" class="form-submit">Submit</button>
</form>
<% } else { %>
<p>You're not ready to join the club, go <a href="/">back</a>.</p>
<%- include('./partials/errors-list') %>
<% } %>
<% } else { %>
<p>Welcome to the Club.</p>
<a href="/">Go back to homepage</a>
<% } %>
<% } %>
